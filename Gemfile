source 'https://rubygems.org'

require 'json'
require 'open-uri'
# versions = JSON.parse(open('https://pages.github.com/versions.json').read)
versions = { "jekyll" => "3.10.0" }  # Manually set the version


gem 'github-pages', group: :jekyll_plugins
gem 'html-proofer'
gem 'jekyll-include-cache'